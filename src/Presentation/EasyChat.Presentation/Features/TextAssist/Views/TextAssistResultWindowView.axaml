<Window xmlns="https://github.com/avaloniaui"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="using:EasyChat.Presentation.Features.TextAssist"
        xmlns:controls="using:EasyChat.Presentation.Features.TextAssist.Controls"
        xmlns:dataConverters="clr-namespace:Avalonia.Data.Converters;assembly=Avalonia.Base"
        xmlns:converters="clr-namespace:EasyChat.Presentation.Foundation.Formatting"
        xmlns:lang="clr-namespace:EasyChat.Presentation.Lang"
        xmlns:sharedControls="clr-namespace:EasyChat.Presentation.Shared.Controls;assembly=EasyChat.Presentation.Shared"
        xmlns:icons="clr-namespace:Material.Icons.Avalonia;assembly=Material.Icons.Avalonia"
        xmlns:shad="clr-namespace:ShadUI;assembly=ShadUI"
        xmlns:md="clr-namespace:LiveMarkdown.Avalonia;assembly=LiveMarkdown.Avalonia"
        x:Class="EasyChat.Presentation.Features.TextAssist.Views.TextAssistResultWindowView"
        x:DataType="vm:TextAssistResultWindowViewModel"
        Width="500" Height="420" MinWidth="380" MinHeight="180"
        WindowDecorations="None" TransparencyLevelHint="Transparent" Background="Transparent"
        Foreground="{DynamicResource ForegroundColor}"
        Topmost="True" ShowInTaskbar="False" CanResize="True" Focusable="False" ShowActivated="False"
        WindowStartupLocation="Manual">
    <Window.Styles>
        <Style Selector="md|MarkdownRenderer.TextAssistResultMarkdown">
            <Style Selector="^ md|MarkdownTextBlock">
                <Setter Property="Foreground" Value="{DynamicResource ForegroundColor}" />
                <Setter Property="FontSize" Value="16" />
            </Style>
        </Style>
    </Window.Styles>
    <Grid>
    <Border Background="{DynamicResource DialogBackgroundColor}" BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" ClipToBounds="False" CornerRadius="{DynamicResource LgCornerRadius}" Padding="12">
        <Border ClipToBounds="True">
        <Grid RowDefinitions="Auto,*,Auto" RowSpacing="8">
            <Grid ColumnDefinitions="*,Auto" ColumnSpacing="8">
                <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <icons:MaterialIcon Kind="{Binding WindowIcon}" Width="19" Height="19"
                                        Foreground="{DynamicResource PrimaryColor}" />
                    <TextBlock Text="{Binding Title}"
                               Classes="Large"
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource ForegroundColor}" />
                </StackPanel>
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="6">
                    <sharedControls:LanguageSelector Width="150"
                                                     Languages="{Binding Languages}"
                                                     SelectedLanguage="{Binding SelectedSourceLanguage, Mode=TwoWay}"
                                                     IsVisible="{Binding ShowLanguageSelector}" />
                    <Button Classes="Ghost" Command="{Binding RetryCommand}"
                            ToolTip.Tip="{x:Static lang:Resources.Retry}" IsEnabled="{Binding !IsBusy}">
                        <icons:MaterialIcon Kind="Refresh" Width="16" Height="16" />
                    </Button>
                    <Button Classes="Primary"
                            Click="OnCopyClick"
                            ToolTip.Tip="{x:Static lang:Resources.Copy}"
                            IsEnabled="{Binding !IsBusy}"
                            HorizontalContentAlignment="Center"
                            VerticalContentAlignment="Center"
                            AutomationProperties.Name="{x:Static lang:Resources.Copy}">
                        <icons:MaterialIcon Kind="ContentCopy" Width="16" Height="16" />
                    </Button>
                    <Button Classes="Icon" Click="OnCloseClick"
                            ToolTip.Tip="{x:Static lang:Resources.Close}"
                            AutomationProperties.Name="{x:Static lang:Resources.Close}">
                        <icons:MaterialIcon Kind="Close" Width="16" Height="16"
                                            Foreground="{DynamicResource ForegroundColor}" />
                    </Button>
                </StackPanel>
            </Grid>
            <Grid Grid.Row="1" MinHeight="100">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel Spacing="12">
                        <StackPanel Spacing="12" IsVisible="{Binding ShowCorrectionResult}">
                            <shad:Card HasShadow="False" Padding="12">
                            <Grid MinHeight="110">
                                    <TextBox x:Name="AnnotatedTextBox"
                                             Text="{Binding SourceText}"
                                             IsReadOnly="True"
                                             AcceptsReturn="True"
                                             TextWrapping="Wrap"
                                             Padding="10,8"
                                             FontFamily="{DynamicResource ContentControlThemeFontFamily}"
                                             FontSize="16"
                                             LineHeight="23"
                                             VerticalContentAlignment="Top"
                                             BorderThickness="0"
                                             Background="Transparent"
                                             PointerMoved="OnOriginalPointerMoved"
                                             PointerExited="OnOriginalPointerExited" />
                                    <controls:CorrectionAnnotationLayer x:Name="AnnotationLayer" IsHitTestVisible="False"
                                             Text="{Binding SourceText}" Issues="{Binding Issues}"
                                             FontFamily="{DynamicResource ContentControlThemeFontFamily}"
                                             FontSize="16" FontWeight="Normal" LineHeight="23" Padding="10,8"
                                             HorizontalAlignment="Stretch" VerticalAlignment="Stretch" />
                                    <Border x:Name="CorrectionHint" IsVisible="False" HorizontalAlignment="Left" VerticalAlignment="Top"
                                            MaxWidth="390"
                                            Background="{DynamicResource DialogBackgroundColor}"
                                            BorderBrush="{DynamicResource BorderColor}"
                                            BorderThickness="1" IsHitTestVisible="False" CornerRadius="{DynamicResource LgCornerRadius}" Padding="16">
                                        <TextBlock x:Name="CorrectionHintText"
                                                   Foreground="{DynamicResource ForegroundColor}"
                                                   TextWrapping="Wrap" />
                                    </Border>
                            </Grid>
                            </shad:Card>
                            <shad:Card HasShadow="False" Padding="12" IsVisible="{Binding HasCorrectedResults}">
                            <StackPanel Spacing="7">
                                <Grid ColumnDefinitions="*,Auto"
                                      ColumnSpacing="8">
                                    <StackPanel Orientation="Horizontal"
                                                Spacing="7">
                                        <icons:MaterialIcon Kind="TextBoxEditOutline"
                                                            Width="16"
                                                            Height="16"
                                                            Foreground="{DynamicResource PrimaryColor}" />
                                        <TextBlock Text="{x:Static lang:Resources.TextAssistCorrectedResult}" FontWeight="SemiBold" />
                                    </StackPanel>
                                    <shad:Loading Grid.Column="1"
                                                  Width="18"
                                                  Height="18"
                                                  IsVisible="{Binding ShowCorrectionLoadingIndicator}" />
                                </Grid>
                                <ItemsControl ItemsSource="{Binding CorrectionVariants}">
                                    <ItemsControl.ItemTemplate>
                                        <DataTemplate>
                                            <Grid ColumnDefinitions="*,Auto"
                                                  ColumnSpacing="8"
                                                  Margin="0,0,0,8">
                                                <StackPanel Spacing="5">
                                                    <TextBlock Text="{Binding Variant, StringFormat=方案 {0}}"
                                                               Opacity="0.62"
                                                               FontWeight="SemiBold" />
                                                    <TextBlock Text="{Binding Text}"
                                                               FontSize="15"
                                                               TextWrapping="Wrap" />
                                                    <TextBlock Text="{Binding Translation}"
                                                               Opacity="0.6"
                                                               TextWrapping="Wrap"
                                                               IsVisible="{Binding Translation, Converter={x:Static dataConverters:StringConverters.IsNotNullOrEmpty}}" />
                                                </StackPanel>
                                                <Button Grid.Column="1"
                                                        Classes="Icon"
                                                        Tag="{Binding}"
                                                        Click="OnCopyCorrectionClick"
                                                        ToolTip.Tip="{x:Static lang:Resources.Copy}"
                                                        VerticalAlignment="Top">
                                                    <icons:MaterialIcon Kind="ContentCopy" Width="16" Height="16" />
                                                </Button>
                                            </Grid>
                                        </DataTemplate>
                                    </ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                            </shad:Card>
                            <StackPanel Spacing="8" IsVisible="{Binding HasCorrectionIssues}">
                                <StackPanel Orientation="Horizontal" Spacing="7">
                                    <icons:MaterialIcon Kind="LightbulbOnOutline" Width="18" Height="18"
                                                        Foreground="{DynamicResource PrimaryColor}" />
                                    <TextBlock Text="{x:Static lang:Resources.TextAssistIssues}" FontWeight="SemiBold" />
                                </StackPanel>
                                <ItemsControl ItemsSource="{Binding Issues}">
                                    <ItemsControl.ItemTemplate><DataTemplate>
                                        <Border Margin="0,0,0,6"
                                                Background="{DynamicResource GhostColor}"
                                                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="{DynamicResource LgCornerRadius}" Padding="16">
                                            <StackPanel Spacing="4">
                                                <TextBlock Text="{Binding DisplayCategory}" FontWeight="SemiBold"
                                                           Foreground="{DynamicResource PrimaryColor}" />
                                                <TextBlock Text="{Binding Message}" TextWrapping="Wrap" />
                                                <TextBlock Text="{Binding Suggestion}" TextWrapping="Wrap" Opacity="0.72" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate></ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </StackPanel>
                        <shad:Card HasShadow="False" Padding="12" BorderThickness="1"
                                BorderBrush="{DynamicResource SuccessColor}"
                                IsVisible="{Binding IsCorrectionCorrect}">
                            <Grid ColumnDefinitions="Auto,*" ColumnSpacing="14">
                                <Border Width="44" Height="44" CornerRadius="22"
                                        Background="{DynamicResource SuccessColor}" VerticalAlignment="Center">
                                    <icons:MaterialIcon Kind="Check" Width="26" Height="26"
                                                        Foreground="{DynamicResource PrimaryForegroundColor}"
                                                        HorizontalAlignment="Center" VerticalAlignment="Center" />
                                </Border>
                                <StackPanel Grid.Column="1" Spacing="4" VerticalAlignment="Center">
                                    <TextBlock Text="{Binding CorrectionStatus}" Classes="Large" FontWeight="SemiBold" />
                                    <TextBlock Text="{Binding CorrectionStatusDetail}" TextWrapping="Wrap" />
                                </StackPanel>
                            </Grid>
                        </shad:Card>
                        <StackPanel Spacing="12" IsVisible="{Binding IsPolish}">
                            <shad:Card HasShadow="False" Padding="12">
                                <TextBlock Text="{Binding Result}" Classes="Large" TextWrapping="Wrap" />
                            </shad:Card>
                            <StackPanel Spacing="8" IsVisible="{Binding HasPolishExplanations}">
                                <StackPanel Orientation="Horizontal" Spacing="7">
                                    <icons:MaterialIcon Kind="BookOpenVariant" Width="18" Height="18"
                                                        Foreground="{DynamicResource PrimaryColor}" />
                                    <TextBlock Text="{Binding PolishExplanationTitle}" FontWeight="SemiBold" />
                                </StackPanel>
                                <ItemsControl ItemsSource="{Binding PolishExplanations}">
                                    <ItemsControl.ItemTemplate><DataTemplate>
                                        <Border Margin="0,0,0,6"
                                                Background="{DynamicResource GhostColor}"
                                                BorderBrush="{DynamicResource BorderColor}" BorderThickness="1" CornerRadius="{DynamicResource LgCornerRadius}" Padding="12">
                                            <StackPanel Spacing="5">
                                                <TextBlock Text="{Binding Category}" FontWeight="SemiBold"
                                                           Foreground="{DynamicResource PrimaryColor}" />
                                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="7" IsVisible="{Binding HasOriginal}">
                                                    <TextBlock Text="{Binding $parent[Window].((vm:TextAssistResultWindowViewModel)DataContext).PolishOriginalLabel}" Opacity="0.62" />
                                                    <TextBlock Grid.Column="1" Text="{Binding Original}" TextWrapping="Wrap" />
                                                </Grid>
                                                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="7" IsVisible="{Binding HasRevised}">
                                                    <TextBlock Text="{Binding $parent[Window].((vm:TextAssistResultWindowViewModel)DataContext).PolishRevisedLabel}" Opacity="0.62" />
                                                    <TextBlock Grid.Column="1" Text="{Binding Revised}" TextWrapping="Wrap" />
                                                </Grid>
                                                <TextBlock Text="{Binding Explanation}" TextWrapping="Wrap" Opacity="0.76" />
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate></ItemsControl.ItemTemplate>
                                </ItemsControl>
                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </ScrollViewer>
                <shad:Card IsVisible="{Binding ShowPlainResult}" HasShadow="False" Padding="12"
                        HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                        <md:MarkdownRenderer Classes="TextAssistResultMarkdown"
                                             MarkdownBuilder="{Binding ResultMarkdown}"
                                             HorizontalAlignment="Stretch"
                                             VerticalAlignment="Top" />
                    </ScrollViewer>
                </shad:Card>
                <shad:Loading Width="24" Height="24" IsVisible="{Binding ShowLoadingIndicator}" HorizontalAlignment="Center" VerticalAlignment="Center" />
            </Grid>
            <!-- Header already owns Retry/Copy/Close; footer only surfaces errors. -->
            <TextBlock Grid.Row="2"
                       Text="{Binding ErrorMessage}"
                       Foreground="{DynamicResource DestructiveColor}"
                       TextWrapping="Wrap"
                       IsVisible="{Binding ErrorMessage, Converter={x:Static dataConverters:StringConverters.IsNotNullOrEmpty}}" />
        </Grid>
        </Border>
    </Border>
        <Border Width="5" HorizontalAlignment="Left" Background="Transparent"
                Cursor="LeftSide" Tag="West" PointerPressed="OnResizePointerPressed" />
        <Border Width="5" HorizontalAlignment="Right" Background="Transparent"
                Cursor="RightSide" Tag="East" PointerPressed="OnResizePointerPressed" />
        <Border Height="5" VerticalAlignment="Top" Background="Transparent"
                Cursor="TopSide" Tag="North" PointerPressed="OnResizePointerPressed" />
        <Border Height="5" VerticalAlignment="Bottom" Background="Transparent"
                Cursor="BottomSide" Tag="South" PointerPressed="OnResizePointerPressed" />

        <Border Width="12" Height="12" HorizontalAlignment="Left" VerticalAlignment="Top"
                Background="Transparent" Cursor="TopLeftCorner" Tag="NorthWest"
                PointerPressed="OnResizePointerPressed" />
        <Border Width="12" Height="12" HorizontalAlignment="Right" VerticalAlignment="Top"
                Background="Transparent" Cursor="TopRightCorner" Tag="NorthEast"
                PointerPressed="OnResizePointerPressed" />
        <Border Width="12" Height="12" HorizontalAlignment="Left" VerticalAlignment="Bottom"
                Background="Transparent" Cursor="BottomLeftCorner" Tag="SouthWest"
                PointerPressed="OnResizePointerPressed" />
        <Border Width="16" Height="16" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Background="Transparent" Cursor="BottomRightCorner" Tag="SouthEast"
                PointerPressed="OnResizePointerPressed">
            <icons:MaterialIcon Kind="ResizeBottomRight" Width="12" Height="12"
                                Foreground="{DynamicResource MutedColor}"
                                HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,2,2"
                                IsHitTestVisible="False" />
        </Border>
    </Grid>
</Window>
